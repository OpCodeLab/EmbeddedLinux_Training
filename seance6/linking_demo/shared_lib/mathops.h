#ifndef MATHOPS_H
#define MATHOPS_H

/* Computation-style functions, provided by the SHARED library (.so). */
double compute_add(double a, double b);
double compute_mul(double a, double b);
long   compute_factorial(int n);

#endif /* MATHOPS_H */
